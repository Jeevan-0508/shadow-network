{
  "name": "shadow-network",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "description": "A persistent synthetic freight-carrier economy that drifts corrupt over time, gets investigated by a BYOK AI council, and keeps a public scoreboard of AI vs fraud.",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc -b --noEmit",
    "tick": "bun run scripts/run-tick.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^5.6.3"
  }
}
